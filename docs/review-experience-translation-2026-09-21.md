# 经验翻译及异步隔离审阅

本次检查经验正文翻译、原始证据隔离、两种模式、前端资源引用及项目回归。

修复：

- Agent 分析正文和经验摘要接入阅读翻译，原始反馈/对话、证据引用、诊断详情、JSON 和编辑框仍保留原文。
- 短英文经验正文切回中文不再受普通聊天长句门槛影响。
- 清除/更换令牌、切换记录和语言时取消旧翻译；响应返回后验证上下文，防止过期结果被接受。
- 翻译响应要求非空字符串数组，不把对象、数字或空值拼接成正文。
- 更新浏览器 API 切换检查：失败诊断保存可能早于任务锁释放，等待真正可重试后再断言按钮可用。

验证：

- 后端全量：1468 passed，212 subtests passed。
- 前端：126 passed，含注销、令牌修改、记录刷新、语言切换期间的旧响应隔离。
- 浏览器：Guided 与 EMVR 的正文翻译、短英文转中文、原始 JSON/草稿保留、失败后显式重试、诊断折叠、API 切换和窄屏通过。
- 2050 段文本和 10 个属性的缓存压力检查通过，没有因缓存淘汰而反复翻译。
- Python 编译、前端 JavaScript 语法、HTML 本地脚本/样式引用检查通过。

不增加提炼 Agent 或分析次数；经验分析仍受原有十次总上限约束。模型语义质量仍需人工审阅。本地验证使用模拟服务商响应，没有调用真实付费 API；不能据此证明线上账户或服务商可用性。
