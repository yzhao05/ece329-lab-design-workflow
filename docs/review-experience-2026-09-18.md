# 经验层及项目回归审阅（2026-09-18）

本次重点检查经验提炼、人工审阅、修订检索、两个 mode 的历史归属、后台重试及前端提交，保留此前稳恒磁场知识修正。

后续更新：反馈总尝试上限已从本文当时的 3 次提高为 10 次，包含跨服务商自动切换；当前行为见[反馈层说明](feedback-layer.md)。下表和验证数量记录本次历史审阅结果。

## 已修复的问题

| 问题 | 修复及回归范围 |
| --- | --- |
| 阶段切换后从顶部反馈，默认阶段与目标回答不一致，历史证据找不到 | 未指定阶段时按目标版本的 handled_stage 定位；明确指定不匹配阶段时保留未知，不擅自重绑定 |
| mode 切换后历史问题按当前 mode 检索经验 | 保存 reported_mode，提炼和修订检索优先使用历史模式；提交时状态单独保留 |
| 切换修正字段只保留页面草稿，先前字段修正未提交 | 将已填写修正同步到 JSON，多个字段共同提交；JSON 无效时保持原字段和草稿 |
| 只学习所选字段，遗漏 JSON 编辑中的有效修订 | 按审阅前后实际差异生成 changes；仅引用仍与当前已启用内容一致的修订 |
| 拒绝、停用、删除请求可夹带正文和范围变更 | 后端拒绝这类混合请求；审批是改写正文和范围的唯一入口 |
| 异常恢复中 queued 任务达到次数上限后仍被永久轮询 | 达到上限直接记为 failed，不再留在待处理集合；保留最多三次尝试及迟到结果保护 |
| DeepSeek 的默认输出预算覆盖经验分析的显式上限 | 将提炼 4200、检查 1400 的上限传到适配层；回归实际 Chat Completions 请求额度 |
| 发布检查未覆盖新增审阅测试，容器缺少元数据引用的 LICENSE | CI 和 Pages 均运行新前端测试；Docker 复制 LICENSE，并排除本机测试与构建产物 |

## 验证方式

- Python 全量回归：1271 项通过，另有 210 个子测试通过；覆盖状态机、模型路由、反馈、报告、知识引用等现有测试。
- 新增针对历史归属、多个字段修订、非批准操作、旧 basis 兼容和耗尽重试终止的测试。
- 30 项前端测试包含审阅页的实际事件处理，覆盖重复点击、多个字段保存、无效 JSON 和退出后的迟到响应；新测试加入 CI。
- 本机浏览器连接测试 API 与 SQLite，验证两种 mode 的反馈、审阅启停、中英文标签及手机布局；模型使用测试替身。
- 检查 HTML/Markdown 本地链接、242 处 Python 相对导入及 9 份知识库 JSON，未发现缺失目标。
- 构建 wheel 并从解包后的安装内容导入新增模块、初始化经验存储；Python 编译检查通过。

## 边界与部署

这些检查覆盖可重复验证的逻辑问题，不等于真实模型的语义归纳总是正确。正反例检查仍是模型审查，标记为 not_replayed；人工批准继续保留。未调用真实付费模型，也未执行线上发布。本机未安装 Docker，容器配方完成静态检查，实际镜像构建由仓库 CI 验证；本机 wheel 构建与导入已通过。

不新增环境变量，不清空数据库。部署需同步更新前后端、重启后端、刷新审阅页。旧自由文本审阅记录仍可读，结构化 basis 兼容为 opinion。完整行为见 [经验层说明](feedback-layer.md)。
