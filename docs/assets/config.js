/*
 * GitHub Pages只托管静态文件，不要在这里填写OpenAI或其他模型密钥。
 * 后端上线后，只填写你自己的工作流API公开地址，例如：
 * API_BASE_URL: "https://api.example.edu"
 */
window.ECE329_CONFIG = Object.freeze({
  API_BASE_URL: "",
  REQUEST_TIMEOUT_MS: 180000,
});
