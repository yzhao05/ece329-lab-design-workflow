# 2026-09-19 工作流复核

本次检查前后端接口与本地引用、阶段推进和反馈重试边界，重点复核经验三状态、人工修订、用量记录以及 EMVR Unity 位置示意图。保留现有未提交功能改动。

## 本次修复

- 清除实验对象后，同步移除旧操作反馈箭头，防止图中残留上一版对象相关的内容。
- 已有位置文字无法解析时显示绘图限制，不把已有回答误报为缺项而重复询问；真正缺失的信息和相互冲突的说明仍可提示补充。
- 检查多个对象的位置循环矛盾，以及“不需要固定观察点”与固定中心观察的冲突；每个组件中心使用独立观察标记。
- 同时处于左侧和前方属于兼容关系，不误报冲突；英文操作中的 `shift` 等单词不会因含 `if` 子串被当作条件句。
- 限制位置解析组合量。超出范围时保留文字并留白，不尝试无限或过量解析。
- 前端校验嵌套快照结构；异常对象、数组或说明不会中断整个聊天界面的渲染。
- 补齐新增静态提示的英文翻译，更新静态资源版本，避免继续加载旧脚本。

## 验证

- 全量 Python：1393 项测试、210 个子测试通过。
- 五组前端测试：70 项通过，覆盖模型选择、反馈、审阅、用量和示意图。
- 本地浏览器与 SQLite：两个 mode 的反馈提交、经验停止/修改/重新启用、维护者鉴权和手机弹窗通过。
- 示意图浏览器验证：Guided 隐藏、EMVR 部分方案、版本过期响应、修改/清除、保留输入草稿、手机布局、中英文、异常快照隔离通过。
- Python/JavaScript 语法检查、本地文档引用和 Python 相对导入检查通过。

反馈分析总尝试次数仍限制为 10 次；存储连续失败 3 次停止后台 worker；前端关闭反馈弹窗或切换设计后停止旧轮询，自动轮询次数有上限。示意图投影只读，不增加阶段门槛，也不会自动发消息或推进阶段。

## 验证范围

浏览器测试使用本机模型替身，没有调用收费 API。测试不能保证任意在线模型输出都正确；自由位置描述未必可绘制，但原文保留且不会因解析失败删除设计内容。费用仍依据服务商返回的 usage 与所配置单价估算，未验证线上账单。

改动不需要新增环境变量。发布时需同步部署前后端，并重启后端；本次未提交或推送 Git，也未部署线上服务。

## 右侧折叠功能追加复核

修复三类状态问题：原生 details 小节在切换设计或模式时重新绑定归属；仅保存用户点击或键盘激活的开合操作，不再把程序重绘产生的 toggle 事件当作用户偏好反复写入；报告同名字段使用独立状态键及唯一 aria-controls 目标，避免控制关系冲突。已有卡片级通用偏好保持不变。

新增四项前端回归验证状态隔离、重复渲染不写存储、取消/过期操作以及浏览器存储不可用时的降级。浏览器验证增加跨设计切换与同名字段，并复测两个 mode、页面刷新、中英文、键盘、手机布局、Unity 示意图和反馈审阅全线路。

本轮结果：1393 项 Python 测试、210 个子测试、74 项前端测试通过；91 个本地文档引用、259 个 Python 相对导入无断联；语法和 diff 检查通过。未发现新增无限重试路径。本轮仅修改前端、测试及文档，无需新增环境变量或修改后端配置；未提交、推送或部署。
